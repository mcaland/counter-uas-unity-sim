# README
### Use instructions
This is a project made in Unity 6000.3.1f1, so make sure to open the project (this repository) in that version.
The build of the project (for Windows) can be found in the `build/` folder.
